VERSION="1.0.0"
BUILDTIME=`date -u +%FT%T%Z`
BUILDHASH=`git rev-parse HEAD`
STATUS=`git status --porcelain`
if [ "$STATUS" ]; then
  BUILDHASH="$BUILDHASH*"
fi

LDFLAGS="-X rakewire/model.Version=${VERSION} -X rakewire/model.BuildTime=${BUILDTIME} -X rakewire/model.BuildHash=${BUILDHASH}"
