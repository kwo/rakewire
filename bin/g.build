g.tools
cd $PROJECT_HOME/go
. $PROJECT_HOME/bin/b.vars
go generate ./src/rakewire/...

if [ "$1" = "linux" ]; then
	export GOOS=linux
	export GOARCH=amd64
fi

go build -ldflags "$LDFLAGS" -o ../rakewire ./src/rakewire/rakewire.go
