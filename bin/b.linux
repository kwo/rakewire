docker run --rm -ti -v $(pwd):/mnt karlostendorf/golang sh -c "cd /mnt && . bashrc && g.build"
cp -v $PROJECT_HOME/rakewire-linux-amd64 $HOME/projects/docker/rakewire
docker run --rm -ti -v $(pwd):/mnt karlostendorf/alpine /mnt/rakewire-linux-amd64 -version
